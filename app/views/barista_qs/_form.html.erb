<%= form_for(@barista_q) do |f| %>
  <% if @barista_q.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@barista_q.errors.count, "error") %> prohibited this barista_q from being saved:</h2>

      <ul>
      <% @barista_q.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

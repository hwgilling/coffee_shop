<%= form_for(@food_q) do |f| %>
  <% if @food_q.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food_q.errors.count, "error") %> prohibited this food_q from being saved:</h2>

      <ul>
      <% @food_q.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
